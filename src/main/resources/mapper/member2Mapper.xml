<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member2">
	<select id="select1" parameterType="String" resultType="member2VO">
		select * from MEMBER2 where mid = #{mid}
	</select>
	<select id="select2" parameterType="member2VO" resultType="member2VO">
		select * from MEMBER2 where gender = #{gender}
	</select>
	<select id="select3" parameterType="String" resultType="member2VO">
		select * from MEMBER2 where mid = #{mid}
	</select> 
	
</mapper>
